##########################################
# TODO 08.04.26
# Project goal: reliably send short text strings between two ADALM-Pluto radios.
# Priority: demo reliability
##########################################

# 0. Demo definition
-- Lock one short success criterion for the course demo and optimize only for that:
   short UTF-8 text payloads
   two Pluto radios
   one documented setup scenario
   repeated successful delivery with retries visible when needed
-- Write down the exact recommended demo setup:
   cable + attenuation or over-the-air
   antenna placement / separation
   TX/RX gains
   carrier, sample rate, symbol rate

# 2. Highest-value work for reliable text transfer
-- Make one repeatable two-radio text demo script / workflow and treat it as the primary deliverable
-- Measure where failures happen in real Pluto runs:
   no burst detected
   burst found but sync fails
   sync succeeds but Gold/header selection is wrong
   payload decode / CRC / FEC fails
   DATA arrives but ACK is lost
-- Save failing IQ captures plus config snapshots so bad runs can be replayed offline
-- Run controlled tuning on real two-radio tests:
   TX gain
   RX gain
   flush-buffer count
   burst-crop settings
   Gold thresholds
   preamble/header search radii
-- Decide on a conservative payload limit for the demo if full datagram capacity hurts reliability

# 3. Tests and validation
-- Add missing low-level tests for:
   scrambler symmetry
   modulation / demodulation round-trip
   Gold detection / removal with realistic offsets
   synchronization-stage behavior on saved captures / fixtures
-- Add one regression path that replays a known-good capture and a known-bad capture through `run_rx_pipeline()`
-- Keep `Hardware_tester.py`, `tests/frame_layout_check.py`, and `tests/tuning_campaign.py` as the reference validation path unless `main.py` is explicitly re-qualified

# 4. Nice to have after the link is stable
-- BER / SER reporting for both simulation and hardware captures
-- Better TUI statistics: retries, packet loss, duplicate suppression, noise floor
-- Cleaner organization of plotting / tuning utilities
-- Richer hardware scripts for TX-only, RX-only, and loopback checks
-- Multiple-radio support

# 5. Explicitly not urgent before the demo works
-- configurable frame formats
-- broader refactors of helper modules
-- advanced channel models beyond what is needed to explain measured Pluto behavior
